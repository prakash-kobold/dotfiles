[user]
        name = Prakash Mohan
        useConfigOnly = true
{{- if not (eq .email "") }}
        email = {{ .email }}
{{- end }}

[color]
        ui = true

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
